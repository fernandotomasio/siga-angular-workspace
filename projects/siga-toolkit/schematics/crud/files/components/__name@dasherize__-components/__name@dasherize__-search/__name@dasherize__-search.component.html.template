<form class="basic-form" [formGroup]="form" (submit)="onSubmit()">
<mat-tab-group  mat-align-tabs="end">
  <mat-tab>
  <ng-template mat-tab-label>
      <mat-icon class="example-tab-icon">search</mat-icon>
      Busca Simples
    </ng-template> 
    <div fxLayout="row wrap">
        <div fxFlex.gt-sm="100" fxFlex="100" class="p-10">
            <mat-form-field>
                <input matInput id="q" type='text' class="form-control" autocomplete="off"
                 formControlName="q" placeholder='Digite um termo para pesquisa...' cdkFocusInitial  />
            </mat-form-field>
        </div>
    </div>
  </mat-tab>
  <mat-tab>
    <ng-template mat-tab-label>
        <mat-icon class="example-tab-icon">zoom_in</mat-icon>
        Busca Avançada
      </ng-template> 
       <% for (let parameter of searchParameters) { %>
        <div fxFlex.gt-sm="100" fxFlex="100" class="p-10">
            <mat-form-field>
                <input matInput type="<% if (parameter.type === 'string') { %>text<% }%><% if (parameter.type === 'number') { %>number<% }%>"
                    class="form-control" formControlName="<%= camelize(parameter.name) %>"  placeholder='<%= capitalize(parameter.label) %>...' />
            </mat-form-field>
        </div>
        <% } %>
        <div fxFlex.gt-sm="100" fxFlex="100" class="p-10">
            <button mat-raised-button type="submit" color="primary">Buscar</button>
        </div>
   </mat-tab>
</mat-tab-group>
</form>
